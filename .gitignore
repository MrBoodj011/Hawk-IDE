node_modules/
dist/
*.vsix
/extensions/hawk-security-ide/resources/hawk-mark.png
/coverage/
/artifacts/
/integrations/burp/build/
/integrations/burp/.gradle/
.DS_Store
*.log
.env
.env.local
.tsbuildinfo
.tsimp/
.vitest/
.tmp/

# Editor / local tooling
.idea/
.vscode/
*.swp

# Local agent/harness config — never commit (may contain target hosts)
.claude/

# Marketing/docs site — developed separately, not tracked in this repo
/web/

# Local agent state & scan output — never commit. Anchored to the repo
# root so they don't also exclude source dirs like src/findings/.
/findings/
/recon/
/.hawk/
bughunter-report-*.md
